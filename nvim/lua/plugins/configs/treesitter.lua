local options = {
  ensure_installed = {
    "lua",
    "python",
    "cpp"
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
